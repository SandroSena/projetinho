import Form from '../../components/Form';
import { LoginContainer } from './styles';

const Login = () => {
  return (
    <LoginContainer>
      <Form />
    </LoginContainer>
  );
};

export default Login;
