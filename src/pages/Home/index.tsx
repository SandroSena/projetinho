import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography variant='h2' sx={{ display: 'flex', justifyContent: 'center' }}>
      Welcome To Projetinho
    </Typography>
  );
};

export default Home;
